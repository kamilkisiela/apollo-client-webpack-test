{
  "name": "apollo-client-webpack-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "(cd w1 && npm run build) && (cd w2 && npm run build)"
  },
  "author": "",
  "license": "ISC"
}
